FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .
RUN pip install -e .

EXPOSE 8000

CMD ["python", "-m", "uvicorn", "ai_diagnostic.api:create_app", "--factory", "--host", "0.0.0.0", "--port", "8000"]
